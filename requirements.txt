# ── InfraBot — Phase 1 + 2 ── requirements.txt ───────────────────────────────
# Installer avec : pip install -r requirements.txt
#
# IMPORTANT : versions épinglées = celles testées et vérifiées sur ce projet.
# crewai 0.28.8 impose langchain 0.1.x (incompatible avec 0.2.x+).
# Ne pas upgrader crewai ou langchain sans tester l'intégration complète.
#
# Compatible Python 3.11 / 3.12 / 3.13

# ── LLM & Orchestration ───────────────────────────────────────────────────────
langchain==0.1.20
langchain-core==0.1.53
langchain-community==0.0.38
langchain-openai==0.1.7
langchain-ollama==0.1.3          # ChatOllama pour llama3.2 local
langchain-chroma==0.1.4          # Intégration ChromaDB officielle

# ── Base vectorielle ──────────────────────────────────────────────────────────
chromadb==0.4.24                 # 0.5.x est incompatible avec ce projet

# ── Embeddings + Re-ranking ───────────────────────────────────────────────────
sentence-transformers==3.0.1     # all-MiniLM-L6-v2 (embeddings) +
                                 # cross-encoder/ms-marco-MiniLM-L-12-v2 (re-ranking)

# ── Chargement de documents ───────────────────────────────────────────────────
pypdf==4.3.1                     # PDF
python-docx==1.1.2               # Word
unstructured==0.22.21            # HTML, TXT, Markdown
beautifulsoup4==4.14.3           # Parsing HTML (Dolibarr wiki)

# ── Interface ─────────────────────────────────────────────────────────────────
streamlit==1.58.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv==1.0.0             # Variables d'environnement
tiktoken==0.7.0                  # Comptage de tokens
loguru==0.7.2                    # Logs structurés
numpy==1.26.4                    # Calculs vectoriels (cache sémantique)
pydantic==2.13.3                 # Validation de données

# ── Phase 2 — Agents CrewAI ───────────────────────────────────────────────────
crewai==0.28.8                   # Dernière version compatible langchain 0.1.x
                                 # crewai >= 0.51 requiert langchain >= 0.3
